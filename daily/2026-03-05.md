# 2026-03-05 开发日志

## 今日目标
- 启动 OpenClaw 自动化贡献流水线（选题→修复→提交→PR）。

## 已完成
- 建立贡献工作区：`contrib/openclaw`
- 选定并修复 issue：`#35476`
- 创建并更新 PR：`#35496`
  - 链接：https://github.com/openclaw/openclaw/pull/35496
  - 标题：fix(telegram): make model picker checkmark provider-aware for duplicate model IDs
  - 变更范围：2 files, +14 / -4

## 技术摘要
- 根因：Telegram model picker 勾选逻辑仅比较 model id，忽略 provider。
- 修复：勾选判断改为 provider+model 联合匹配（保留 provider 缺失的兼容分支）。
- 测试：补充“同 model id、不同 provider 不应同时勾选”的回归用例。

## 风险与阻塞
- 本地环境缺少 `pnpm` 全局命令（可通过 corepack 使用）。
- 已切换到 API 自动 PR 流程，后续将继续沿用。

## Next
- 按小而实策略继续选择高 merge 概率 bug。
- 维持并行 2-3 条修复分支。

## 14:52 子任务交付（#35564）
- 选题：`#35564`（Bug：Discord `agentComponents` 被 strict zod schema 误拒绝）
- 分支：`fix/discord-agentcomponents-35564`
- 提交：`af0cd68d9`
- PR：`#35625` https://github.com/openclaw/openclaw/pull/35625

### 变更
- `src/config/zod-schema.providers-core.ts`
  - 在 `DiscordAccountSchema` 中补充 `agentComponents` 字段 schema（`enabled?: boolean`）
- `src/config/config.discord.test.ts`
  - 新增回归测试：允许 `channels.discord.agentComponents.enabled` 通过 `validateConfigObject`

### 说明
- 按要求采用轻量流程：未再执行全量安装；通过静态审查确认变更文件语法结构与测试语义合理。

## 16:41 子任务交付（#35580）
- 选题：`#35580`（Bug：WhatsApp allowFrom 拒绝时错误信息误导为 E.164 校验/缺失 target）
- 分支：`fix/whatsapp-35580`
- 提交：`48d4d1992`
- PR：`#35778` https://github.com/openclaw/openclaw/pull/35778

### 变更
- `src/whatsapp/resolve-outbound-target.ts`
  - 当目标不在 `channels.whatsapp.allowFrom` 允许名单中时，返回专门的 allowFrom policy 错误信息，引导用户正确配置（而不是 generic missing-target 错误）。
- `src/whatsapp/resolve-outbound-target.test.ts`
  - 更新测试：在 implicit/heartbeat/broadcast 模式下，拒绝应包含 "allowFrom policy" 文案。

### 验证
- 运行：`pnpm test:fast src/whatsapp/resolve-outbound-target.test.ts`

## 16:57 子任务交付（#35777）
- 选题：`#35777`（Bug：BlueBubbles debounce flush 在 `text: null` 时 `.trim()` 崩溃，导致该窗口内消息丢失）
- 分支：`fix/bluebubbles-35777`
- PR：`#35800` https://github.com/openclaw/openclaw/pull/35800

### 变更
- `extensions/bluebubbles/src/monitor-debounce.ts`
  - 合并 debounce entries 时将 `message.text` 视为可空，统一 coalesce 为 `""` 再 `.trim()`，避免 flush 批次因为 TypeError 中断。

### 验证
- 运行：`node_modules/.bin/vitest run extensions/bluebubbles/src/monitor-debounce.test.ts`

## 17:10 子任务交付（#35795）
- 选题：`#35795`（Bug：Sub-agent completion messages ignore channel messageToolHints）
- 分支：`fix/subagents-35795`
- PR：`#35815` https://github.com/openclaw/openclaw/pull/35815

### 变更
- `src/agents/system-prompt.ts`
  - minimal prompt mode（subagent/cron）现在也会注入 channel 的 `messageToolHints`，以 `## Channel Formatting` 小节形式呈现。
  - 修复 subagent 在不支持 Markdown 的渠道（例如 Synology Chat / Campfire HTML）里发出 raw Markdown 的问题。
- `src/agents/system-prompt.test.ts`
  - 增加回归用例：minimal mode 在提供 `messageToolHints` 时应包含 `## Channel Formatting`。

### 验证
- 运行：
  - `npm test -- --run src/agents/system-prompt.test.ts`
  - `npm test -- --run src/agents/subagent-announce-dispatch.test.ts`
  - `npm test -- --run src/agents/subagent-announce-queue.test.ts`
  - `npm test -- --run src/agents/subagent-announce-queue.test.ts`
  - `npm test -- --run src/agents/system-prompt-stability.test.ts`
  - `npm test -- --run src/agents/system-prompt-params.test.ts`

## 17:31 子任务交付（#35620）
- 选题：`#35620`（Bug：browser profile driver `openclaw` 被 strict zod schema 误拒绝，仅接受 legacy `clawd`）
- 分支：`fix/browser-driver-35620`
- 提交：`e5c0e54bf`
- PR：`#35849` https://github.com/openclaw/openclaw/pull/35849

### 变更
- `src/config/zod-schema.ts`
  - `browser.profiles.*.driver` 允许值增加 `"openclaw"`（并保留 legacy `"clawd"` + `"extension"`）
- `src/config/schema.help.ts`
  - 更新帮助文本，注明 `openclaw` 是当前值，`clawd` 为 legacy
- `src/config/schema.help.quality.test.ts`
  - 更新允许值列表断言
- `src/config/config.browser-profiles-driver.test.ts`
  - 新增回归测试：`validateConfigObject` 应接受 driver: `openclaw`

### 验证
- 运行：`pnpm -s vitest -c vitest.unit.config.ts run src/config/config.browser-profiles-driver.test.ts`

## 17:59 子任务交付（#35721）
- 选题：`#35721`（Bug：Chrome 对 `--disable-blink-features=AutomationControlled` 显示 unsupported switch 警告）
- 分支：`fix/browser-35721`
- 提交：`e779cd80e`, `cb54aa9d4`
- PR：`#35885` https://github.com/openclaw/openclaw/pull/35885

### 变更
- `src/browser/chrome.ts`
  - 支持在 `browser.extraArgs` 中加入 `"!--disable-blink-features=AutomationControlled"` 来移除 OpenClaw 默认注入的该启动参数，以消除 Chrome 的警告。
  - 将 Chrome 启动参数组装抽出为 `buildOpenClawChromeArgs(...)`，并把 `!--` 视为指令（不会透传给 Chrome），避免把 `!--...` 当作未知 flag 传给浏览器。
- `src/browser/chrome.test.ts`
  - 新增回归测试：确认 `!--` negation 能移除 OpenClaw 注入的 arg 且不会被转发。

## 18:26 子任务交付（#35881）
- 选题：`#35881`（Bug：Cron job store / run logs 创建为 0644 而非 0600）
- 分支：`fix/cron-perms-35881`
- 提交：`801530b3c`
- PR：`#36814` https://github.com/openclaw/openclaw/pull/36814

### 变更
- `src/cron/store.ts`
  - 写入时 best-effort `chmod(storePath, 0o600)`
  - `.bak` 备份文件 copy 后 best-effort `chmod(..., 0o600)`
- `src/cron/run-log.ts`
  - append + prune 后 best-effort `chmod(logPath, 0o600)`，避免 umask 导致 0644
- 测试
  - `src/cron/store.test.ts` / `src/cron/run-log.test.ts` 增加 POSIX 权限断言

### 验证
- 运行：`pnpm -s vitest -c vitest.unit.config.ts src/cron/store.test.ts src/cron/run-log.test.ts`

## 22:56 子任务交付（#36170 / #36298）
- 选题：`#36170`（Bug：`chat.inject` 在 transcriptPath 指向的文件缺失时硬失败）
- 分支：`fix/chat-36170`
- 提交：`5753c767c`
- PR：`#36298` https://github.com/openclaw/openclaw/pull/36298

## 09:24 子任务交付（#36170 / #36959）
- 选题：`#36170`（Bug：`chat.inject` 在 transcriptPath 指向的文件缺失时 ENOENT 硬失败）
- 分支：`fix/chat-inject-create-missing-36170`
- 提交：`b871e21bd`
- PR：`#36959` https://github.com/openclaw/openclaw/pull/36959

### 变更
- `src/gateway/server-methods/chat.ts`
  - `chat.inject` 调用 transcript append helper 时将 `createIfMissing` 设为 `true`，从而在 ENOENT 时自动 mkdirp + 创建 transcript 文件并继续写入。

### 验证
- 运行：`pnpm -s vitest -c vitest.gateway.config.ts run src/gateway/server-methods/chat.inject.parentid.test.ts`

> 注：该修复与此前同 issue 的其它 PR 属于同根因重复修复（不同分支/提交），这里保留一条记录即可。

## 08:xx 子任务交付（#36869）
- 选题：`#36869`（Bug：voice-call streaming + outbound 时 initial message 可能在 stream connect 前被 TwiML `<Say>` fallback 消耗，导致实际未在 stream 上播报）
- 分支：`fix/voice-call-36869`
- 提交：`132a8fbf0`
- PR：未创建（此环境无 GitHub auth）
  - 建议创建链接：https://github.com/openclaw/openclaw/compare/main...clawbie:fix/voice-call-36869?expand=1

## 08:23 子任务交付（#35397）
- 选题：`#35397`（Bug：`OPENCLAW_DOCKER_APT_PACKAGES` 在 `setup-podman.sh` 中被忽略）
- 分支：`fix/podman-35397`
- 提交：`a8a7d3c05`
- PR：`#36914` https://github.com/openclaw/openclaw/pull/36914

## 08:40 子任务交付（#36799）
- 选题：`#36799`（Bug：session-logs skill 的 `*.jsonl` glob 会漏掉 `.jsonl.reset.<timestamp>` 归档 transcripts）
- 分支：`fix/session-logs-36799`
- 提交：`b87cbc424`
- PR：`#36925` https://github.com/openclaw/openclaw/pull/36925

### 变更
- `skills/session-logs/SKILL.md`
  - 文档中 glob 从 `*.jsonl` 改为 `*.jsonl*`
  - 补充三类 transcript 文件说明：active / reset / deleted

### 验证
- 本地用临时文件复现并验证：`rg -l "keyword" /tmp/session-logs-test/*.jsonl*` 可同时命中 `.jsonl` 与 `.jsonl.reset.*`

### 变更
- `setup-podman.sh`
  - `podman build` 增加 `--build-arg OPENCLAW_DOCKER_APT_PACKAGES=...`，行为与 `docker-setup.sh` 对齐。
- `test/setup-podman.apt-packages.test.sh`
  - 新增轻量脚本测试：静态断言 build arg 已接入（不依赖 podman、无需安装依赖）。

### 验证
- 运行：`./test/setup-podman.apt-packages.test.sh`

### 变更
- `extensions/voice-call/src/manager.ts`
  - streaming enabled 时跳过 `call.answered` 钩子里的 initial message 播放，避免 outbound answered 早于 stream 注册导致 TwiML `<Say>` fallback 抢跑。
- `extensions/voice-call/src/manager.streaming-initial-message.test.ts`
  - 新增回归单测：streaming enabled 时不应在 answered 阶段触发 speak；disabled 时应触发。

### 验证
- 运行：`pnpm -s vitest -c vitest.extensions.config.ts run extensions/voice-call/src/manager.streaming-initial-message.test.ts`

## 23:26 子任务交付（#36263）
- 选题：`#36263`（Bug：cron.update 丢弃 `payload.fallbacks`，导致模型 fallback 无法持久化）
- 分支：`fix/cron-36263`
- 提交：`663ce6a23`
- PR：`#36350` https://github.com/openclaw/openclaw/pull/36350

### 变更
- `src/cron/service/jobs.ts`
  - `mergeCronPayload()` 支持合并 `patch.fallbacks`
  - `buildPayloadFromPatch()` 在 kind-change 重建时保留 `fallbacks`
- `src/cron/service.issue-regressions.test.ts`
  - 增加断言：cron.update 传入 fallbacks 后应持久化到 payload

### 验证
- 运行：`pnpm vitest -c vitest.unit.config.ts src/cron/service.issue-regressions.test.ts`

## 23:42 子任务交付（#36323）
- 选题：`#36323`（Bug：Telegram raw markdown tables 渲染为管道/横线墙）
- 分支：`fix/hooks-36326`（命名历史原因；实际变更为 Telegram markdown tables 默认渲染）
- 提交：`e5f937273`, `e0b96ae50`
- PR：`#36370` https://github.com/openclaw/openclaw/pull/36370

### 变更
- `src/config/markdown-tables.ts`
  - Telegram 默认 markdown table mode 设为 `code`，将表格转换为 code block（Telegram 才能正确展示）。
- `src/config/markdown-tables.defaults.test.ts`
  - 新增单测覆盖 Telegram 默认值。

### 验证
- 运行：`vitest -c vitest.unit.config.ts run src/config/markdown-tables.defaults.test.ts`

## 00:08 子任务交付（#36353）
- 选题：`#36353`（Bug：Kimi Coding provider 忽略用户配置的 baseUrl，导致 404）
- 分支：`fix/kimi-coding-36353`
- PR：`#36403` https://github.com/openclaw/openclaw/pull/36403

### 变更
- `src/agents/models-config.providers.ts`
  - 在 kimi-coding 作为 implicit provider（由环境变量/Profiles API key 自动注入）时，若用户在显式 providers 配置中指定了 `baseUrl`，则优先保留该值。
- `src/agents/models-config.providers.kimi-coding.test.ts`
  - 新增回归用例：显式配置的 `baseUrl` 应覆盖内置默认值。

### 验证
- 运行：`npx vitest run src/agents/models-config.providers.kimi-coding.test.ts`

## 00:19 子任务交付（#36385）

## 06:31 子任务交付（#36353）
- 选题：`#36353`（Bug：models merge mode 会保留旧的 models.json baseUrl，导致用户更新 baseUrl 不生效）
- 分支：`fix/models-36353`
- PR：`#36804` https://github.com/openclaw/openclaw/pull/36804

### 变更
- `src/agents/models-config.ts`
  - `mergeWithExistingProviderSecrets()` 仅保留 secret（`apiKey`），不再把 `baseUrl` 当作 secret 覆盖用户配置。
- `src/agents/models-config.fills-missing-provider-apikey-from-env-var.test.ts`
  - 更新并新增回归用例：merge mode 下应保留 `apiKey`，但当 config 提供新 baseUrl 时不应被旧 models.json 覆盖。

### 验证
- 运行：`pnpm -s vitest -c vitest.scoped-config.ts`（scoped include 该测试文件）

## 06:09 子任务交付（#36736）
- 选题：`#36736`（Bug：Discord channel-edit 未透传 appliedTags，导致论坛/媒体线程标签编辑无效）
- 分支：`fix/discord-36736`
- PR：`#36760` https://github.com/openclaw/openclaw/pull/36760

### 变更
- `src/channels/plugins/actions/discord/handle-action.guild-admin.ts` / `src/agents/tools/discord-actions-guild.ts`
  - 解析并透传 `appliedTags` 到 channelEdit payload
- `src/discord/send.channels.ts`
  - `editChannelDiscord()` 将 `appliedTags` 映射为 Discord REST 字段 `applied_tags`
- `src/discord/send.types.ts`
  - `DiscordChannelEdit` 增加 `appliedTags?: string[]`
- 测试
  - 新增 `test/discord/send.edits-channel.test.ts`：断言提供 appliedTags 时会发送 `applied_tags`，否则不发送

### 验证
- `pnpm -s vitest -c vitest.unit.config.ts run test/discord/send.edits-channel.test.ts`


## 05:40 子任务交付（#36736）
- 选题：`#36736`（Bug：Discord `channel-edit` 未透传 `appliedTags`，无法给论坛贴更新标签）
- 分支：`fix/discord-36736`
- 提交：`7841ae88f`
- PR：`#36760` https://github.com/openclaw/openclaw/pull/36760

### 变更
- `src/channels/plugins/actions/discord/handle-action.guild-admin.ts`
  - `channel-edit` 读取 `appliedTags` 并传给 `handleDiscordAction`
- `src/agents/tools/discord-actions-guild.ts`
  - `channelEdit` 同步透传 `appliedTags`
- `src/discord/send.types.ts` / `src/discord/send.channels.ts`
  - `DiscordChannelEdit` 支持 `appliedTags?: string[]`，并映射到 Discord API 的 `applied_tags`
- 测试
  - 更新 `src/channels/plugins/actions/actions.test.ts` 断言 appliedTags forwarding
  - 新增 `src/discord/send.edits-channel.test.ts` 覆盖 REST payload

### 验证
- 运行：`pnpm -s vitest -c vitest.channels.config.ts run src/discord/send.edits-channel.test.ts`
- 选题：`#36385`（Bug：skill-creator quick validator 应拒绝空 name/description）
- 分支：`fix/skill-creator-36385`
- PR：`#36426` https://github.com/openclaw/openclaw/pull/36426

### 变更
- `skills/skill-creator/scripts/quick_validate.py`
  - `name` / `description` 现在会明确拒绝空字符串或 YAML null（例如 `name:` / `description:`）
- `skills/skill-creator/scripts/test_quick_validate.py`
  - 新增回归测试覆盖空 name / 空 description

### 验证
- 运行：`python3 -m unittest -v skills/skill-creator/scripts/test_quick_validate.py`

## 00:30 子任务交付（#36170）
- 选题：`#36170`（Bug：`chat.inject` 在 transcriptPath 指向的文件缺失时硬失败）
- 分支：`fix/chat-inject-36170`
- 提交：`821fbb039`
- PR：`#36437` https://github.com/openclaw/openclaw/pull/36437

### 变更
- `src/gateway/server-methods/chat.ts`
  - `chat.inject` 调用 transcript append helper 时将 `createIfMissing` 设为 `true`，从而在 ENOENT 时自动创建 transcript 文件并继续写入。
- `src/gateway/server-methods/chat.inject.createifmissing.test.ts`
  - 新增回归测试：当 session entry 指向的 transcript 文件不存在时，`chat.inject` 应创建文件并写入 message。

### 验证
- 运行：`npx --no-install vitest run --config vitest.gateway.config.ts src/gateway/server-methods/chat.inject.createifmissing.test.ts`

## 01:06 子任务交付（#36088）
- 选题：`#36088`（Bug：TUI 发送消息时 `--deliver` 路由没有按 CLI client 规则继承 main session 的 deliverable routes）
- 分支：`fix/tui-36088`
- 提交：`0779ca510`
- PR：`#36475` https://github.com/openclaw/openclaw/pull/36475

### 变更
- `src/tui/gateway-chat.ts`
  - 将 TUI gateway chat client 的 `mode` 设为 `CLI`，避免被当作 Webchat/UI client 导致 gateway 路由强制 internal。
- `src/gateway/server-methods/chat.tui-deliver-routing.test.ts`
  - 新增回归测试：CLI-mode client 在配置 main session 且开启 deliver 时，应允许继承 deliverable routes。

### 验证
- 运行：`npm test -- --run src/gateway/server-methods/chat.tui-deliver-routing.test.ts`

## 01:25 子任务交付（#36477）
- 选题：`#36477`（Bug：Slack plugin 未传播 `mediaLocalRoots`，导致本地文件上传失败）
- 分支：`fix/slack-36477`
- PR：`#36496` https://github.com/openclaw/openclaw/pull/36496

### 变更
- `src/plugin-sdk/slack-message-actions.ts`
  - Slack message action 的 send 路径加入 `mediaLocalRoots: ctx.mediaLocalRoots`，把 agent/workspace 允许目录传给下游。
- `src/agents/tools/slack-actions.ts`
  - Slack tool handler 读取并透传 `mediaLocalRoots` 到 `sendSlackMessage`。
- `src/slack/actions.ts`
  - `sendSlackMessage` 支持透传 `mediaLocalRoots` 到 `sendMessageSlack`。
- `src/agents/tools/slack-actions.test.ts`
  - 新增回归测试：media send 时必须把 `mediaLocalRoots` 传给 `sendSlackMessage`。

### 验证
- 运行：`pnpm -s vitest run src/agents/tools/slack-actions.test.ts`

### 备注
- 已将 PR 分支 rebase 到最新 `upstream/main`，确保 PR 仅包含 Slack 相关 1 个提交，避免混入其它修复提交。

## 01:52 子任务交付（#36508）
- 选题：`#36508`（Bug：多渠道/多插件场景下 `config.get` RPC 可能因 schema cache key 过大触发 `RangeError: Invalid string length`）
- 分支：`fix/config-36508`
- PR：`#36523` https://github.com/openclaw/openclaw/pull/36523

### 验证
- `pnpm -s vitest -c vitest.unit.config.ts src/config/schema.test.ts`

## 02:22 子任务交付（#36008）
- 选题：`#36008`（Bug：Ubuntu 24.04 上 `systemctl --user is-enabled` 在 unit 缺失时有时返回 code 1 + "not-found"，被误判为 systemctl 不可用）
- 分支：`fix/systemd-is-enabled-36008`
- 提交：`50a6a2718`
- PR：`#36562` https://github.com/openclaw/openclaw/pull/36562

### 变更
- `src/daemon/systemd.test.ts`
  - 新增回归用例：`is-enabled` 在 `code=1` 且 stdout 为 `not-found` 时也应视为未启用（返回 false），避免安装向导误报 `systemctl is-enabled unavailable`。

### 验证
- 运行：`npm test -- --run src/daemon/systemd.test.ts`

### 变更
- `src/config/schema.ts`
  - `buildMergedSchemaCacheKey` 不再把完整 `configSchema` / `configUiHints` 序列化进 cache key；改为只使用 plugin/channel 的轻量 identity metadata，并对 JSON 做 sha256 digest，避免超大字符串/内存占用。
- `src/config/schema.test.ts`
  - 新增回归测试：构造大 schema/hints 输入时不应崩溃。

### 验证
- 运行：`pnpm -s vitest -c vitest.unit.config.ts src/config/schema.test.ts`

## 02:43 子任务交付（#36170）
- 选题：`#36170`（Bug：`chat.inject` 在 transcript jsonl 缺失时硬失败）
- 分支：`fix/gateway-chat-inject-36170`
- 提交：`5efe0912a`
- PR：`#36579` https://github.com/openclaw/openclaw/pull/36579

### 变更
- `src/gateway/server-methods/chat.ts`
  - `chat.inject` 调用 transcript append helper 时将 `createIfMissing` 设为 `true`，从而在 ENOENT 时自动创建 transcript 文件并继续写入。
- `src/gateway/server-methods/chat.inject.missing-transcript.test.ts`
  - 新增回归测试：当 session entry 指向的 transcript 文件不存在时，`chat.inject` 应创建文件并写入 message。

### 验证
- 运行：`pnpm -s vitest run --config vitest.gateway.config.ts src/gateway/server-methods/chat.inject.missing-transcript.test.ts`

## 04:59 子任务交付（#36581）
- 选题：`#36581`（Bug：Feishu media extension 因 SDK request option 类型变化导致 TypeScript typecheck 失败）
- 分支：`fix/feishu-36581`
- PR：`#36691` https://github.com/openclaw/openclaw/pull/36691

### 变更
- `extensions/feishu/src/media.test.ts`
  - 更新回归测试：断言 upload/download API 调用不再传入顶层 `timeout` 字段（该字段已不被新版本 SDK typings 接受）。
  - 说明：实际超时控制由 `createFeishuClient(..., httpTimeoutMs)` 的 timeout-aware HTTP wrapper 统一负责。

### 验证
- 运行：`pnpm -s vitest extensions/feishu/src/media.test.ts --run`

## 05:33 子任务交付（#36508）
- 选题：`#36508`（Bug：多渠道/多插件场景下 `config.get` RPC 可能因 schema cache key 过大触发 `RangeError: Invalid string length`）
- 分支：`fix/config-schema-cachekey-36508`
- PR：`#36733` https://github.com/openclaw/openclaw/pull/36733

### 变更
- `src/config/schema.ts`
  - `buildMergedSchemaCacheKey` 由直接 `JSON.stringify({ plugins, channels })` 改为：
    - 只取 plugin/channel 的 identity metadata（id/name/label/description）构造 JSON
    - 对 JSON 做 sha256 digest 作为 cache key
  - 避免把巨大的 JSON schema / UI hints 作为 Map key，防止出现 `RangeError: Invalid string length`。
- `src/config/schema.test.ts`
  - 新增回归测试：构造大 schema 输入，`buildConfigSchema(...)` 不应崩溃。

### 验证
- 运行：`node ./node_modules/vitest/vitest.mjs run src/config/schema.test.ts`


## 2026-03-06 补充（#36736 / PR #36760）
- 在 `fix/discord-36736` 分支上追加提交：`0da933f93`
  - 将 `send.edits-channel.test.ts` 从 `src/discord/` 移到 `test/discord/`，并调整为走 unit runner（避免走 channels runner）。
- PR：#36760 https://github.com/openclaw/openclaw/pull/36760
- 验证：`pnpm -s vitest -c vitest.unit.config.ts run test/discord/send.edits-channel.test.ts`

## 2026-03-06 子任务交付（#36869）
- 选题：`#36869`（Bug：Twilio outbound conversation + streaming 下，初始消息在 media stream 连接前被 <Say> fallback 消耗，导致 WebSocket 端无内容可播）
- 分支：`fix/voice-call-36869`
- 提交：`132a8fbf0`
- 变更：
  - streaming enabled + Twilio 时，`call.answered` 路径不再 speak initial message（交由 media stream onConnect 在 stream 注册后播报）
  - 新增 targeted 单测：`extensions/voice-call/src/manager.streaming-initial-message.test.ts`
- 测试：未运行（仓库无 node_modules；本环境按规则不执行依赖安装）
- PR：未创建（此环境无 GitHub auth；无法 push / gh pr create）。可用 compare URL：
  - https://github.com/openclaw/openclaw/compare/main...clawbie:fix/voice-call-36869?expand=1

## 2026-03-06 子任务交付（#36170 / PR #36959）
- 选题：`#36170`（Bug：`chat.inject` 在 transcriptPath 指向的文件缺失时硬失败）
- 分支：`fix/chat-inject-create-missing-36170`
- 提交：`b871e21bd`
- PR：#36959 https://github.com/openclaw/openclaw/pull/36959

### 变更
- `src/gateway/server-methods/chat.ts`
  - `chat.inject` 现在用 `createIfMissing: true` 追加 transcript，从而在 ENOENT 时自动 mkdirp + 创建 transcript 文件并继续写入。

### 验证
- 运行：`pnpm -s vitest -c vitest.gateway.config.ts run src/gateway/server-methods/chat.inject.parentid.test.ts`

### 变更
- `extensions/voice-call/src/manager.ts`
  - 当 `streaming.enabled` 且 provider 为 Twilio 时，`call.answered` 路径不再触发 `speakInitialMessage`，避免与 TwiML `<Connect><Stream>` 交付竞争。
  - 初始消息改由 media stream `onConnect` 路径在 stream register 后播报。
- `extensions/voice-call/src/manager.notify.test.ts`
  - 新增回归用例：Twilio + streaming enabled 时，answered 不应直接触发 provider.playTts。

### 验证
- 未运行测试（仓库无 node_modules；本环境禁止执行依赖安装）。
